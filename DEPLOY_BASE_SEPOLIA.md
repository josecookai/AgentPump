# Base Sepolia 测试网智能合约部署指南

本指南将帮助你完成 AgentPump 智能合约在 Base Sepolia 测试网的部署。

## 📋 目录

1. [准备工作](#1-准备工作)
2. [环境变量配置](#2-环境变量配置)
3. [安装和编译](#3-安装和编译)
4. [运行测试](#4-运行测试)
5. [部署合约](#5-部署合约)
6. [验证合约](#6-验证合约)
7. [部署后测试](#7-部署后测试)
8. [常见问题](#8-常见问题)

---

## 1. 准备工作

### 1.1 环境要求

- **Node.js**: 18.0.0 或更高版本
- **npm**: 9.0.0 或更高版本
- **Hardhat**: 已通过 `npm install` 安装
- **Base Sepolia ETH**: 用于支付 gas 费用（至少 0.01 ETH）

### 1.2 获取 Base Sepolia 测试网 ETH

**方法1: Coinbase Faucet**（推荐）
- 访问: https://www.coinbase.com/faucets/base-ethereum-sepolia-faucet
- 使用 Coinbase 账号登录
- 选择 Base Sepolia 网络
- 领取测试 ETH

**方法2: 其他 Faucet**
- Base Sepolia Faucet: https://www.alchemy.com/faucets/base-sepolia
- 或搜索 "Base Sepolia faucet"

**注意**: 确保钱包已切换到 Base Sepolia 网络（Chain ID: 84532）

### 1.3 准备部署账户

1. 创建一个新的钱包或使用现有钱包
2. 导出私钥（**仅用于测试网，不要在主网使用**）
3. 确保账户有足够的 Base Sepolia ETH

---

## 2. 环境变量配置

### 2.1 创建 `.env` 文件

在项目根目录创建 `.env` 文件：

```bash
cp .env.example .env
```

### 2.2 配置必需的环境变量

编辑 `.env` 文件，填写以下内容：

```bash
# 部署者私钥（不带 0x 前缀）
PRIVATE_KEY=your_private_key_without_0x_prefix

# 签名者地址（用于生成 launchToken 签名，可以是部署者地址）
SIGNER_ADDRESS=0xYourSignerAddress

# Base Sepolia Uniswap V2 Router 地址
# 注意：Base Sepolia 可能没有 Uniswap V2，见下方说明
UNISWAP_V2_ROUTER=0xYourUniswapRouterAddress

# Basescan API Key（用于合约验证，可选）
BASESCAN_API_KEY=your_basescan_api_key

# 是否自动验证合约（true/false）
VERIFY=false
```

### 2.3 Base Sepolia Uniswap Router 地址

**重要**: Base Sepolia 测试网可能没有部署 Uniswap V2。

**解决方案**:

**选项A: 查找 Base Sepolia Uniswap Router**
- 检查 Base 官方文档
- 或联系 Base 团队获取测试网地址

**选项B: 部署 Mock Router**（推荐用于测试）
- 测试文件中包含 Mock Router 实现
- 可以部署一个简单的 Mock Router 用于测试

**选项C: 使用其他 DEX**
- 如果有其他 DEX 在 Base Sepolia 上，可以使用其 Router

**当前建议**: 对于测试，可以先使用 Mock Router 或零地址，Graduation 功能可以稍后测试。

### 2.4 获取 Basescan API Key（可选）

如果需要自动验证合约：

1. 访问 https://basescan.org
2. 注册账号并登录
3. 进入 API-KEYs 页面
4. 创建新的 API Key
5. 复制 API Key 到 `.env` 文件

---

## 3. 安装和编译

### 3.1 安装依赖

```bash
# 在项目根目录
npm install
```

**预期输出**: 所有依赖包安装成功

### 3.2 编译智能合约

```bash
npx hardhat compile
```

**预期输出**:
```
Compiled 2 Solidity files successfully
```

**如果编译失败**:
- 检查 Solidity 版本是否匹配（0.8.19）
- 检查 OpenZeppelin 依赖是否正确安装
- 查看错误信息并修复

**编译成功后**: 会生成 `artifacts/` 和 `cache/` 目录

---

## 4. 运行测试

### 4.1 运行所有测试

```bash
npx hardhat test
```

**预期输出**: 所有测试通过

**测试覆盖**:
- ✅ 部署测试
- ✅ Launch Token 测试（签名验证、费用检查）
- ✅ Buy Token 测试
- ✅ Sell Token 测试
- ✅ 动态费用测试
- ✅ 错误处理测试

### 4.2 如果测试失败

**常见问题**:

1. **Mock Router 部署失败**
   - 检查测试文件中的 Mock Router 实现
   - 确保 Hardhat 网络正常运行

2. **签名验证失败**
   - 检查测试中的签名生成逻辑
   - 确保 chainId 正确（84532 for Base Sepolia）

3. **Gas 估算失败**
   - 确保 Hardhat 网络配置正确
   - 检查合约代码是否有问题

**修复后**: 重新运行测试直到全部通过

---

## 5. 部署合约

### 5.1 检查网络配置

确认 `hardhat.config.js` 中 Base Sepolia 配置正确：

```javascript
baseSepolia: {
  url: "https://sepolia.base.org",
  accounts: [process.env.PRIVATE_KEY],
}
```

### 5.2 部署当前模式（AgentBondingCurve.sol）

```bash
npx hardhat run scripts/deploy.js --network baseSepolia
```

**预期输出**:
```
🚀 Deploying AgentPumpFactory to Base...
Using signer address: 0x...
Using Uniswap V2 Router: 0x...
✅ AgentPumpFactory deployed to: 0x...
   Signer address: 0x...
   Uniswap V2 Router: 0x...
```

**记录以下信息**:
- Factory 合约地址
- 部署交易哈希
- 部署者地址

### 5.3 部署 Pump.fun 模式（可选）

如果你想同时部署 Pump.fun 模式：

```bash
npx hardhat run scripts/deploy-pumpfun.js --network baseSepolia
```

**注意**: 两个模式可以同时部署，使用不同的合约地址。

### 5.4 部署后检查

1. **在 Basescan 上查看合约**
   - 访问 https://sepolia.basescan.org
   - 搜索你的合约地址
   - 确认部署成功

2. **检查合约状态**
   - 确认 `signerAddress` 设置正确
   - 确认 `UNISWAP_V2_ROUTER` 设置正确

---

## 6. 验证合约

### 6.1 自动验证（推荐）

如果设置了 `VERIFY=true` 和 `BASESCAN_API_KEY`，部署脚本会自动验证合约。

**验证过程**:
1. 等待 5 个区块确认
2. 自动调用 Hardhat verify 插件
3. 在 Basescan 上验证源代码

**验证成功后**: 在 Basescan 上可以查看和验证合约源代码

### 6.2 手动验证

如果自动验证失败，可以手动验证：

```bash
npx hardhat verify --network baseSepolia \
  <FACTORY_ADDRESS> \
  <SIGNER_ADDRESS> \
  <UNISWAP_ROUTER_ADDRESS>
```

**示例**:
```bash
npx hardhat verify --network baseSepolia \
  0x1234567890123456789012345678901234567890 \
  0xabcdefabcdefabcdefabcdefabcdefabcdefabcd \
  0x9876543210987654321098765432109876543210
```

### 6.3 验证常见问题

**问题1: Contract already verified**
- **解决**: 合约可能已经验证过，可以忽略

**问题2: Verification failed - Unable to verify**
- **解决**: 
  - 检查 API Key 是否正确
  - 等待更多区块确认后重试
  - 检查构造函数参数是否正确

**问题3: Network not supported**
- **解决**: 确保 Basescan 支持 Base Sepolia 验证

---

## 7. 部署后测试

### 7.1 基础功能测试

**1. 连接钱包到 Base Sepolia**
- 使用 MetaMask 或其他钱包
- 切换到 Base Sepolia 网络（Chain ID: 84532）
- 确认有足够的测试 ETH

**2. 测试 Launch Token**
- 访问前端应用（如果已部署）
- 或使用 Ethers.js 脚本直接调用合约
- 需要 Moltbook 验证（见下方说明）

**3. 测试 Buy Token**
- 调用 `buy()` 函数
- 发送 ETH 购买 tokens
- 确认 tokens 正确 mint

**4. 测试 Sell Token**
- 调用 `sell()` 函数
- 卖出 tokens 获取 ETH
- 确认 tokens 正确 burn

**5. 查看价格和费用**
- 调用 `getCurrentPrice()` 查看当前价格
- 调用 `getCreatorFeeBps()` 查看动态费用
- 确认费用计算正确

### 7.2 高级功能测试

**Dev Buy 功能**:
- 在 launchToken 时设置 `devBuyAmount > 0`
- 确认 dev buy 正确执行
- 确认费用正确计算

**图片上传**（前端功能）:
- 测试图片上传和预览
- 确认 base64 编码正确

**社交链接**（前端功能）:
- 测试 Twitter、Telegram、Website 链接
- 确认链接格式正确

**Symbol 验证**:
- 测试 symbol 必须以 "MOLTPUMP" 结尾
- 确认无效 symbol 被拒绝

**Graduation 机制**:
- 需要达到 20 ETH collateral
- 可以通过多次买入达到阈值
- 确认自动毕业到 Uniswap V2

### 7.3 Moltbook 验证说明

Launch Token 需要 Moltbook 验证：

1. **获取 Moltbook API Key**
   - 访问 https://moltbook.com
   - 注册账号并创建 Agent
   - 获取 API Key

2. **验证流程**
   - 在 Moltbook 上发布包含验证码的帖子
   - 前端调用 `/api/verify` 端点
   - 后端验证帖子并生成签名
   - 使用签名调用 `launchToken()`

**如果 Moltbook API 不可用**:
- 可以暂时跳过验证（仅用于测试）
- 或实现替代验证方案

---

## 8. 常见问题

### 8.1 部署问题

**问题1: Insufficient funds**
```
Error: insufficient funds for gas
```

**解决方案**:
- 确保账户有足够的 Base Sepolia ETH
- 检查 gas price 设置
- 尝试降低 gas limit

**问题2: Invalid Uniswap Router**
```
Error: Invalid Uniswap router
```

**解决方案**:
- 确认 Base Sepolia 上的 Uniswap Router 地址
- 或使用 Mock Router 用于测试
- 检查 Router 地址格式是否正确

**问题3: Network error**
```
Error: network error
```

**解决方案**:
- 检查 RPC URL 是否正确（https://sepolia.base.org）
- 检查网络连接
- 尝试使用其他 RPC 端点

### 8.2 验证问题

**问题1: Contract already verified**
- **解决**: 合约已经验证过，可以忽略

**问题2: Verification failed**
- **解决**: 
  - 检查 API Key 是否正确
  - 等待更多区块确认
  - 检查构造函数参数

**问题3: Unable to verify**
- **解决**: 
  - Basescan 可能不支持 Base Sepolia
  - 可以稍后手动验证
  - 或使用其他验证方法

### 8.3 测试问题

**问题1: Mock Router 部署失败**
- **解决**: 检查测试文件中的 Mock Router 实现

**问题2: 签名验证失败**
- **解决**: 
  - 检查签名生成逻辑
  - 确认 chainId 正确（84532）
  - 确认 nonce、deadline 正确

**问题3: Gas 估算失败**
- **解决**: 
  - 检查合约代码
  - 检查网络配置
  - 尝试增加 gas limit

### 8.4 功能问题

**问题1: Launch Token 失败**
- **检查**: 
  - 签名是否正确
  - Moltbook 验证是否通过
  - 是否支付了足够的费用

**问题2: Buy/Sell 失败**
- **检查**: 
  - Token 是否已毕业
  - 余额是否充足
  - Slippage 设置是否合理

**问题3: Graduation 不触发**
- **检查**: 
  - Collateral 是否达到 20 ETH
  - Uniswap Router 是否正确配置
  - 是否有足够的 gas

---

## 9. 下一步

部署成功后：

1. **更新前端配置**
   - 更新 `frontend/.env` 中的 `NEXT_PUBLIC_FACTORY_ADDRESS`
   - 部署前端到 Railway（见 `DEPLOY_RAILWAY.md`）

2. **测试完整流程**
   - 创建 token
   - 买入/卖出
   - 测试 graduation

3. **监控和优化**
   - 监控 gas 使用
   - 优化合约代码
   - 收集用户反馈

4. **准备主网部署**
   - 完成所有测试
   - 进行安全审计
   - 准备主网部署

---

## 10. 有用的链接

- **Base Sepolia Explorer**: https://sepolia.basescan.org
- **Base Sepolia Faucet**: https://www.coinbase.com/faucets/base-ethereum-sepolia-faucet
- **Base 文档**: https://docs.base.org
- **Hardhat 文档**: https://hardhat.org/docs
- **Basescan API**: https://basescan.org/apis

---

## 11. 支持

如果遇到问题：

1. 查看 `TROUBLESHOOTING.md` 获取详细故障排除指南
2. 检查 GitHub Issues
3. 联系项目维护者

---

**部署完成后，请记录以下信息**:
- ✅ Factory 合约地址
- ✅ 部署交易哈希
- ✅ 部署时间
- ✅ 网络（Base Sepolia）
- ✅ 合约是否已验证

这些信息将用于前端配置和后续维护。
